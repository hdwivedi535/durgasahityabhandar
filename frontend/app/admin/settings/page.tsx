export default function AdminSettingsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Settings</h1>
      <p className="mt-2 text-muted">System configuration — coming in later phases.</p>
    </div>
  );
}
